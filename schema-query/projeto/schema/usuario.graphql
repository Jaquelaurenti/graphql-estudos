type Perfil{
    id: ID
    nome: String
}

# Trabahando com Enum

enum UsuarioStatus {
    ATIVO
    INATIVO
    BLOQUEADO
}


type Usuario{
    id: Int
    nome: String!
    email: String!
    idade: Int
    salario: Float
    vip: Boolean
    perfil: Perfil
    status: UsuarioStatus
}